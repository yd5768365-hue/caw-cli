# Core dependencies
click>=8.0.0
rich>=13.0.0
numpy>=1.21.0
pyyaml>=6.0
jinja2>=3.0.0
pint>=0.22
requests>=2.25.0
chromadb>=0.4.0
sentence-transformers>=2.2.0

# GUI dependencies (PySide6)
PySide6>=6.5.0

# Full features (geometry processing)
pythonocc-core>=7.7.0
meshio>=5.0.0
vtk>=9.0.0
matplotlib>=3.5.0
pandas>=1.3.0

# Tools dependencies
# (meshio and matplotlib already included above)

# Optimization features
# (matplotlib and numpy already included above)

# SSH features
paramiko>=3.0.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
pre-commit>=3.0.0
